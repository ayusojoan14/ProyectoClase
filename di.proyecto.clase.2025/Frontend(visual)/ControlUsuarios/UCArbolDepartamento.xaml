<UserControl x:Class="di.proyecto.clase._2025.Frontend_visual_.ControlUsuarios.UCArbolDepartamento"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:di.proyecto.clase._2025.Frontend_visual_.ControlUsuarios"
             mc:Ignorable="d"
             xmlns:mat="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:ent="clr-namespace:di.proyecto.clase._2025.Backend.Modelos"
             x:Name="ucArbolDepartamento"
             Loaded="ucArbolDepartamento_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <TextBlock Text="ADMINISTRACIÓN DE DEPARTAMENTOS"
               FontSize="28"
               Foreground="DarkViolet"
               HorizontalAlignment="Center"
               Margin="10" />

        </Grid>
        <!-- ESTE ES EL ARBOL DE LA IZQUIERDA PUESTO EN LA IZQUIERDA *ATENCION* FALTAS DE ORTOGRAFIA -->
        <Grid DockPanel.Dock="Left" Width="300">
            <TreeView x:Name="treeDepartamento" ItemsSource="{Binding listadepartamento}"
               SelectedItemChanged="treeDepartamento_SelectedItemChanged">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type ent:Departamento}"
                                       ItemsSource="{Binding Usuarios}">
                        <StackPanel Orientation="Horizontal">
                            <mat:PackIcon Kind="Cigarette" Width="16" Foreground="MediumVioletRed" />
                            <TextBlock Text="{Binding Nombre}" Margin="10,0,0,0"/>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat=" ({0})">
                                        <Binding Path="Articulos.Count" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>

        <!-- AQUI TERMINA EL ARBOL DE LA IZQUIERDA -->






        <Grid x:Name="panelCentral">
            <DataGrid x:Name="departamento" AutoGenerateColumns="False"
          Style="{StaticResource MahApps.Styles.DataGrid}"
          ColumnWidth="*" CanUserAddRows="False" SelectedItem="{Binding departamentoseleccionado}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre de departamento" Binding="{Binding Nombre}" />
                    <DataGridTextColumn Header="Nombre de usuario" Binding="{Binding TipoNavigation.Usuarios}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</UserControl>
